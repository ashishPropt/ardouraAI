# This file intentionally left as a placeholder.
# Real environment variables belong in .env (gitignored).
# See .env.template for the full list of required variables.
#
# Quick start:
#   cp .env.template .env
#   # then edit .env with your real credentials
